# 对齐审计 —— tf-dpfl 与官方 Bad-PFL / FedRep / 原文献

> **门槛：本表所有行关闭之前，不跑任何 P2 run。**（DECISIONS.md D-006）
> 关闭 = 状态为 `done`（已对齐并有 L1 测试）或 `deviate`（有意偏离，理由已写明、你签过字）。
> `harness/status.py` 读本表的「状态」列：只要还有一行不是 `done` / `deviate`，
> 所有 `requires: [audit]` 的运行组都显示 **blocked**。
>
> **状态取值**（反引号里的词是机器读取的）：
> `open` 未决定 · `align` 已决定对齐、待实现 · `deviate` 有意偏离（理由见「决定」列）· `done` 已对齐且有测试
> （第二节「还没查的」里，`done` = 已经查完、结论已转进对应的 A 行）

## 参考源（逐字读过的）

| 源 | 位置 | 读取方式 | 版本标识 |
|---|---|---|---|
| Bad-PFL 官方 | `github.com/fmy266/Bad-PFL`，main 分支 | `raw.githubusercontent.com`，2026-09-24 | `fba.py` etag `d65deddbd62a…` |
| 读过的文件 | `fba.py` `client.py` `server.py` `main.py` `fl_process.py` `utils.py` `generator.py` `pfl.py` | — | — |
| 重读（A2 会话，2026-09-25） | 11 个文件按 raw 重新下载（含 `resnet.py` `trigger.py` `event_emitter.py`）；`fba.py` `client.py` `main.py` 的 etag 与 A1 相同。另读了 `fl_process.py` / `server.py` / `utils.py` / `pfl.py` 的训练与划分部分 | raw | 同上 |
| 重读（A1 会话，2026-09-25） | 同上 8 个文件按 raw 重新下载逐行核对；`fba.py` etag 仍为 `d65deddbd62a…`，未变。**`client.py` 的 `PMClient` / `PMPoisonClient` 从未被 import**（`main.py:6` 只 import `BasicClient, PoisonClient`）→ 官方实际执行的只有 FedBN 单模型（F-022） | raw | `client.py` etag `69e2b095c234…`、`main.py` `93855051ac20…` |
| 重读（A3 会话，2026-09-25/26） | `resnet.py`（152 行，etag `a280f45176c9…`，与 A2 同）、`trigger.py`（`a05f89e85727…`）、`event_emitter.py`（`2fe2b78cb738…`）、`pfl.py`（`6d16a4c626b1…`）逐行读完；另重读 `server.py` `main.py` `fl_process.py` `utils.py` `client.py` `fba.py`。见 A17、A18、A27、A28 | raw | 见左 |
| ~~**没读的**~~ | ~~`resnet.py` `trigger.py` `event_emitter.py`~~ → A3 会话已读 | — | A17/A18 → `done` |
| FedRep 原作者 | `github.com/LittleStory233/FedRep`（README 写明为 Collins 等人 ICML'21 官方代码；`lgcollins/FedRep` 的 raw 路径 404）；读过 `models/Update.py`、`utils/options.py`、`main_fedrep.py`。**A3 会话**（`master` 分支）加读 `models/test.py`、`models/Nets.py`，并逐行核对 `main_fedrep.py:60-200`、`Update.py:527-640` | raw，2026-09-25 | — |
| Bad-PFL 论文 | ICLR 2025 正文 + 附录（28 页，用户 2026-09-25 提供的 PDF；**未入库**，10.5 MB 超过单文件红线）。引用写成「论文 p.N」。A2 会话用户重新上传，用 pypdf 抽取 p.4、p.7、p.8、p.13、p.14、p.18 核对 | 用户上传 | — |
| PFLlib（旁证） | `github.com/TsingZ0/PFLlib`：`system/flcore/clients/clientrep.py`、`servers/serverrep.py`、`serverbase.py`、`main.py`。**A3 会话**重读 `clientrep.py`（`set_parameters` 在 `:86-88`）、`serverrep.py:23-51`、`serverbase.py:103-150` | raw，2026-09-25 | — |
| HierFAVG 官方（A3 新增） | `github.com/LuminLiu/HierFL`（README 写明为 Liu 等人 *Client-Edge-Cloud Hierarchical Federated Learning* 的实现）：`hierfavg.py` `edge.py` `cloud.py` `client.py` `average.py` `options.py` `models/initialize_model.py` | raw，2026-09-25 | `master` |
| Keras 源码（A3 旁证） | TF 2.15.1 自带的 `keras/src/applications/resnet.py`（stride-2 卷积前显式 `ZeroPadding2D`，`:356,441`）、`keras/src/layers/normalization/batch_normalization.py`（默认 momentum 0.99，`:172`） | scratch venv | TF 2.15.1 |
| **读不到的**（A3） | arXiv、OpenReview、github.com 的 HTML 与 API 在本环境被网络策略 403 → **HierFAVG 论文（arXiv 1905.06641）与 FedRep 论文原文都没读**，A21 / A26 / A28 只以官方代码为证 | — | — |
| 数值探针（A3） | TF 2.15.1 CPU 跑 `models/cnn.py:build_resnet10` 与 `tf.nn.conv2d`，结果与复现片段见 FINDINGS F-033（**不入库**的 scratch 脚本，只回传数字） | 本地 | TF 2.15.1 |

TF 侧行号：A01–A06、A14、A24 基于 `317fbb5`（A1 会话重核；`fedavg/main.py` 相对 `df015b2` 在前 40 行内多了 2 行、`load_config` 里多了 12 行，其余 TF 文件未变）；A07–A13、A15、A16、A22、A23、A25 中 A2 会话新写的行号基于 `8fc613b`；A3 会话新写的行号（A17、A21、A26–A29、D01–D06）基于 `8b19c0b`；其余行基于 `df015b2`。

---

## 一、已找到的差异（2026-09-24 初查 A01–A16；2026-09-25 读论文后加 A22–A23；同日 A1 会话拍板 A01–A06、A14，并加 A24；同日 A2 会话拍板 A07–A13、A15、A16、A22、A23，并加 A25；2026-09-25/26 A3 会话加 A26–A29）

| ID | 项 | 官方（出处） | tf-dpfl（出处） | 状态 | 决定 / 理由 | 怎么验证 |
|---|---|---|---|---|---|---|
| A01 | ξ 的构造 | `fba.py:6-22` `pgd_attack`：随机起点 `uniform_(-ε,ε)`(:8) → clamp[0,1](:9) → 在**起点处**求梯度(:14-16) → 一步 `α·sign(grad)`(:17) → η clamp ±ε(:18) → clamp[0,1](:19)；`:55` 加 δ 之后**不再** clamp；ε=α=4/255。论文 Eq.6（p.6）只写 `σ·sign(∇ₓL(F(x),y))`：在 x 处、无随机起点、无 clamp —— **论文与代码不一致** | `client_badpfl.py:85-91`：在 x 处 `σ·sign(∇ₓCE(model(x),y))`，无随机起点、无任何 clamp | `done` | **D-014：对齐官方代码**（论文 Eq.6 不采纳）：U(−σ,σ) 起点 → clamp 合法像素范围 → 在起点处求梯度 → +σ·sign → 投影 ±σ → clamp；+δ 之后不 clamp。生成器训练 / 训练期投毒 / 评估三处都用它。TF 在标准化空间：[0,1] 换成逐通道 `[(0−μ)/s, (1−μ)/s]`、σ 换成 σ/s（sign 在正仿射变换下不变）。量级：官方 ξ 只有约 50% 像素满幅、均值 0.75σ，TF 现为处处 σ（F-019） | L1：① 线性 softmax 小模型（输入梯度可解析）+ 注入固定 u → 与 numpy 参考实现逐元素相等；② x+ξ 落在合法范围；③ \|ξ\| ≤ σ_norm；④ 反向锚点：内点 \|ξ\|=σ 的比例 ≈ 0.5（现实现 = 1.0 → 改前失败）。**A4 已实现（c54a2092）**：开关 `backdoor.badpfl_xi: pgd`；L1 `tests/test_badpfl_official.py`（与 numpy 参考逐元素相等、内点满幅比例 ≈0.5，fgsm = 1.0 为反向锚点） |
| A02 | 评估时 ξ 用哪个模型 | `fba.py:60-64` 循环里每个恶意端都覆盖一次 `eval_func` → 留下的是 `main.py:95` `shuffle(clients)` 之后**最后一个**恶意端（≈ 开跑时随机选一个、全程固定）；`:53` 用它的 `client.local_model`，即它最后一次训练后的陈旧模型 | `main.py:164-177` → `client_badpfl.py:164-172` `eval_trigger(model,…)` 在**被评估模型**上白盒算 ξ | `done` | D-004：主 ASR 对齐官方，白盒 ξ 另记一列作上界。**D-015**：setup 时用 `default_rng([seed, …])` 从恶意端里固定选**一个**、全程不变，它的 id 与所在 edge 写进 `[设定]` 行；用它最后一次训练后的模型（与官方一样陈旧） | L1：换受害者模型，主 ASR 路径的 ξ 不变；选中者 = rng 的解析结果；`[设定]` 行能被 `collect_metrics` 解析。**A4 已实现（d08bdea6）**：开关 `backdoor.eval_xi_model: fixed_attacker`；`main.select_eval_attacker` 按 `default_rng([seed, 0xA02])` 选，`[设定5]` 自描述；ξ 用它的 PM（D-033）；白盒列 `[ASRwb]`；L1 `tests/test_eval_integration.py` |
| A03 | 投毒量 | `fba.py:48` 逐样本伯努利 `rand() <= ρ`；`:49-50` 一个都没抽中就原样返回。论文 Eq.4（p.3）是 α 加权损失（期望意义） | `client_badpfl.py:146` `k = int(round(n·ρ))`，每 batch 恰好 k 个 | `done` | **D-016：对齐伯努利**，用客户端自己的 seeded rng。现实现在 batch 32 下有取整偏差：ρ=0.2 → 0.1875（−6.3%）、ρ=0.05 → +25%、ρ=0.02 → +56%（F-020） | L1：10⁴ 个 batch 的投毒比例均值落在 ρ±3σ_binom；每 batch 计数方差 > 0（反向锚点：现实现 ρ=0.2 时恒为 6）；ρ=0 → 输入逐字节不变；ρ=1 → 全部投毒。**A4 已实现（c54a2092）**：开关 `backdoor.poison_sampling: bernoulli`（`attack/poison_mask.py`）；L1 `tests/test_pixel_space_and_poison_mask.py`、`tests/test_badpfl_official.py` |
| A04 | 生成器训练数据 | `fba.py:36` `clean_data, clean_label = client.fetch_data()`，但 `client.py:124-125` `PoisonClient.fetch_data` 覆写成 `poison_func(...)` → 实际拿到**按 ρ 已投毒**、标签为 target 的批次；且 `:54` 的内层 δ 没 detach，梯度穿过两次生成器。论文 Eq.7（p.6）：E_{(x,y)∼D_i}，**干净** x、真实 y —— **论文与代码不一致** | `main.py:472,510-525` 动态投毒保留干净 ds；`client_badpfl.py:100` 用 `self.dataset` 干净批次 | `deviate` | **D-017：对齐论文 Eq.7（干净数据），偏离官方代码**（= 现状，A4 不改代码）。理由：论文写得明确；官方变量名 `clean_data` 表明意图是干净数据，却被 `PoisonClient.fetch_data` 覆写，疑似 bug。代价：F-013 所述「ρ=1 时 PGD 与生成器相互抵消」不会复现，大 ρ 下与官方行为不同 | L1（守住现状）：生成器训练批次中被投毒的比例 = 0、标签 = 真实标签 |
| A05 | BN 训练/推理模式 | 6 个调用点：#1 训 G 时 G 前向 → train（G **从不** `.eval()`）；#2 训 G 时 F 前向 → `eval()`（`fba.py:32`）；#3 训练期投毒 G 生成 δ → train（本 batch 32 张的统计量）；#4 训练期投毒 F 算 ξ → train（`client.py:35` 先 `.train()` 再 `fetch_data`）；#5 评估时 G 生成 δ → train（测试 batch 32，`drop_last`）；#6 评估时攻击者 F 算 ξ → **取决于客户端列表顺序**（攻击者自己被评估前停在 train，`utils.py:33` 之后是 eval） | 除 #1 `training=True`（`client_badpfl.py:109`）外全部 `training=False`（`:89,95`）；评估时整个探针（≤ `asr_max_samples`）一次喂进生成器 | `done` | **D-018**：#3 #4 #5 对齐官方（G 在投毒 / 评估时用 batch 统计，评估按 32 张分块，尾块怎么处理在 A4 定——官方是 `drop_last` 丢弃；训练期求 ξ 时 F 用训练模式）；#1 #2 已一致；**#6 偏离官方，用推理模式**，理由：官方此处是顺序依赖的偶然行为（还会在评估中改写攻击者的 running stats）。现状问题：G 按 batch 统计优化、按 moving stats 使用，Keras momentum 0.99 下首次 30 步后仍残留 74% 初值（F-021，对 ASR 的数值影响无证据） | L1：用记录器包住 model 与 generator，断言 6 个调用点的 `training` 标志与本行逐项相同；评估时生成器每次调用的 batch ≤ 32。**A4 已实现（c54a2092、d08bdea6）**：开关 `backdoor.badpfl_bn_mode: official`；评估尾块用本探针样本循环补足到 32、只计真实样本（A4 用户拍板）；L1 `tests/test_badpfl_official.py`（6 个调用点的训练标志、每块恰好 32） |
| A06 | ASR 定义 | `main.py:127-134` + `utils.py:29-48`：**不过滤**，目标类样本也计入；对**全部**客户端（含 10 个恶意端）的 `local_model` 逐客户端 ASR 等权求均值，只在训练结束时评一次。论文 p.7：「ASR over triggered samples for clients' personalized models on their test sets」 | `backdoor_eval.py:43-68`、`:71-97` `_collect_eligible` 只数非目标类；良性端与恶意端分开报（`:274-283`），逐客户端等权平均 | `done` | **D-019**：四列都报（过滤 / 不过滤 × 全体 / 仅良性，均为逐客户端等权平均）；**主指标 = 过滤 × 仅良性**（= 现 `local_benign_asr`）；「不过滤 × 全体」作为与论文可比的列。理由：3-B 刻意改变各 edge 的 y_t 占比（C2 约 30%、C3 < 1%），不过滤的口径会被占比机械抬高；恶意端自身 ASR 在 ρ=1 时是常数预测 | L1：构造探针（t 个目标类 + m 个其他类）：恒预测 target → 四列都为 1；恒预测真标签 → 不过滤 = t/(t+m)、过滤 = 0；加入恶意端，验证「全体」与「仅良性」的解析均值。**A4 已实现（d08bdea6）**：开关 `evaluation.asr_columns: four_way` → `[ASR4]` 行；计数在 `attack/asr_counting.py`；L1 `tests/test_eval_pm_and_asr_columns.py` |
| A07 | 聚合权重 | `server.py:4-10` `agg_avg`：**不加权**平均；论文 p.8「the FedAvg aggregator, where each client's contribution is treated equally during aggregation」。官方客户端等大小（`main.py:65-66`：每端 500 / 100 张），所以等权 ≡ 样本加权 | edge：`aggregation/fedavg.py:17-27` 按参与客户端样本数加权；cloud：`server.py:179-195` → 同一函数，按 `edge.n_samples`（`edge_server_base.py:36`，**全部成员**样本数，不只是参与者）加权。seed42 下客户端 133–909 张，权重是均值的 0.30–2.02 倍（FINDINGS F-028） | `deviate` | **D-027**：保留样本加权（HierFAVG 的标准形式）。与官方的差别只来自客户端不等大小；S3 的新划分要求客户端等大小（D-027），届时两者数值上等价 | 守卫（S3 实现）：P2 划分下全体客户端 `n_samples` 相等 |
| A08 | 学习率 | `main.py:57` `SGD(lr=0.1)`，**无调度器**；论文 p.7「SGD with a learning rate of 0.1」，未提衰减 | `client_base.py:117-126` `lr0·0.992^cloud_round`；FedRep head `hier_fedrep.py:75-85` `head_lr_rep=0.005`，同样按云轮衰减。两者来自 `9d303262` / `27d7d0dc`（修 FedRep 故障，FINDINGS F-030） | `deviate` | **D-023**（取代 D-010）：保留调过参的 0.992 与 head 0.005，但改为**按有效轮衰减**：`0.1·0.992^t_eff`、head `0.005·0.992^t_eff`，t_eff = 截至本 edge 轮已完成的有效轮数（R_edge=1 时恰为现在的云轮号）→ flat 逐字节不变，F-003 的 flat/HFL 混淆消失。偏离官方的常数 lr。做成配置开关（默认 = 现行为）；**D-029 可行性实验通过后改 `deviate`** | L1：同一有效轮上 flat 与 R_edge ∈ {5, 40} 的 base / head lr 相同；flat 下新旧开关给出的 lr 序列逐元素相同；集群：D-029。**A4 已实现开关（7e43a426）**：`training.lr_round_axis: effective`；L1 `tests/test_schedule_alignment.py`。pilot 已登记（`pilot/registry.yaml` 的 D029 组），**D-029 通过后改 `deviate`**。**pilot（`2853433`）D-029 `pass` → `deviate`**：陈旧 pm_acc 末 10 点 flat 0.8985（门槛 0.7367）、2edge 0.9093（门槛 0.7297），两格都 `converged`（有效轮 150 / 210）；FINDINGS F-045、`harness/pilot_a4.py` |
| A09 | 本地训练量 | `main.py:32` + `fl_process.py:29-30`：每轮 **15 步** × batch 32（每端 500 张、`drop_last` → 恰 1 epoch）；论文 p.7「batch size of 32 for 15 steps (roughly one epoch)」、1000 轮；p.18「when the training round reaches 500, both the models and the attack methods converge」 | `training.local_epochs: 5`（完整 epoch，≈68 步） | `deviate` | **D-024**：维持 ep5。**总本地训练量已与论文相当**：论文每端 1000 × 10% × 15 步 ≈ 100 epoch（500 轮收敛 ≈ 50 epoch）；P1 每端 150–300 有效轮 × 10% × 5 epoch ≈ 75–150 epoch（FINDINGS F-029）。差别在 τ（两次聚合之间的本地步数 ≈68 vs 15）与通信轮数，不在训练总量。ep5 是 Stage B 按墙钟标定选出的（`experiments/calibration/RESULTS.md` §二） | — |
| A10 | 预处理 / 增强 | `main.py:58` 只有 `ToTensor()`：[0,1]、不标准化、不增强；论文未提 | `dataset.py:168` 逐通道标准化；`partition.py:38-40` 翻转 + 裁剪（每 epoch 是否重新采样见 A25） | `deviate` | **D-025**：主协议保留标准化 + 增强。「官方预处理」（无标准化、无增强）作**对比实验组 G7**（PLAN §4），检验用户的假设「官方设定降低了攻击难度」。前提：`client_badpfl.py:53-56` 的 ε/σ 换算与 `attack/triggers.py` 必须跟随标准化开关，否则 ξ 放大 3.8–4.1×（FINDINGS F-027） | L1（G7 的前提）：关标准化时模型输入 == `x/255`、`_atk_eps_norm == 4/255`（反向锚点：现在 ≈ 0.0635） |
| A11 | 数据划分 | `main.py:60-84` + `utils.py:51-85`：每客户端类先验 Dir(0.5)，train / test **各自**按同一先验抽，每客户端**等大小**（500 / 100）；numpy 未播种（F-024） | 合并 60k（`main.py:701`）→ 逐类 Dir(0.5·1₁₀₀)（`partition.py:147-183`）、客户端**不等大**（seed42：133–909）→ 客户端内切 25% test | `deviate` | **D-027**：不照抄官方划分 —— 标签偏斜同量级（5 个 seed：客户端平均标签熵 1.75–1.77 vs 1.65–1.68 nats；论文 p.14 的 60.88 分辨不出两种方案，F-031），为对齐而改没有收益。**真正的问题是大小不等**：名义 10% 的恶意端，在 P1 各布点下的数据占比为 0.092–0.142（F-028），而布点正是自变量 → **S3 的新划分硬要求客户端等大小** | S3 的 L1：每客户端样本数相等；恶意端数据占比 = n_malicious / n_clients |
| A12 | FedRep 本地训练 | 上游代码只有 FedBN（`pfl.py:3-24`）。**论文 p.13 附录 A**：「For the PFL methods, we use the same training configuration as that of the local models to train personalized models」→ head 与 body **各** SGD lr=0.1、batch 32、15 步（p.7） | `client/hier_fedrep.py`：先 head `plocal_epochs=1`（lr `head_lr_rep=0.005`）后 body `local_epochs=5`（lr 0.1·0.992^r）；head = 最后一层 Dense，BN 进聚合 | `deviate` | **D-024（取代 D-012）**：维持 P1 现状 —— head `plocal_epochs: 1`、`head_lr_rep: 0.005`，body `local_epochs: 5`，lr 按 D-023。理由：总本地训练量已与论文相当（F-029）；head 的独立低 lr 是 `27d7d0dc` 针对 head 过拟合调出来的（F-030）。偏离论文附录 A「the same training configuration」。**原决定**：D-012：对齐 Bad-PFL 论文：head、body 各 15 步，lr 同为 0.1，batch 32，去掉单独的 head lr。原作者（4:1、momentum 0.5、wd 1e-4、lr 0.01）与 PFLlib（1:1、lr 0.005、batch 10）只作旁证，见 FINDINGS F-016。「head = 哪几层、BN 算不算 body」论文没写 → **A3 定（D-030）**：head = 末层 `Dense(num_classes)` 的 kernel + bias（FedRep 原作者 `main_fedrep.py:70` 去掉的是 `fc3`，PFLlib 用 `model.fc`，TF `cnn.py:217`）；BN 的归属见 A27，训练顺序见 A26，评估用的 PM 见 A28 | —（维持现状，无需改代码） |
| A13 | 数值精度 | `client.py:59-73` `autocast` 只包训练的前向 + loss；每客户端一个 `GradScaler`（初始 scale 2¹⁶，溢出即跳过该步）；PGD 与生成器训练不在 autocast 内（fp32） | fp32 | `deviate` | **D-028**：保持 fp32。TF 的 `mixed_float16` 是全局 Keras policy，会同时改到末层 softmax（本仓库 `from_logits=False`）、BN、生成器与 PGD 的输入梯度；照官方的作用域复刻要逐模型设 policy，收益没有证据。代价：不复现 GradScaler 可能跳过的前几步（机制推断，无数值证据） | grep 守卫：`fedavg/` 中无 `mixed_float16` |
| A14 | 生成器结构 | `generator.py:6-40`：Conv / ConvT 均为 k4 s2 **p1**，全部带 bias；末层 ConvT → Tanh，**无 BN**；torch 默认 BN eps 1e-5、kaiming_uniform(a=√5) 初始化；输入是 [0,1] 图。论文 p.14 表 5 末层写「ConvTranspose2d + BatchNorm2d + Tanh」—— **论文与代码不一致** | `models/autoencoder.py:31-43`：`padding="same"`、前 7 层 `use_bias=False`、末层带 bias 无 BN、Keras BN eps 1e-3、glorot_uniform；输入是标准化图 | `done` | **D-020：以官方代码为准**：保留 `same`（偶数输入、k4 s2 时 SAME 的 pad_total=(out−1)·2+4−in=2 → 前后各 1，与 p1 等价，用 L1 证明）；加 bias；BN eps 1e-5；init 用 `VarianceScaling(1/3, "fan_in", "uniform")`（= torch 默认；ConvT 的 fan_in 两边都按输出通道数算）；生成器输入先反标准化到 [0,1]（零填充的语义才一致）；末层不加 BN（表 5 不采纳） | L1：同权重下 SAME ≡ `ZeroPadding2D(1)`+VALID、ConvT SAME ≡ VALID+`Cropping2D(1)`，逐元素相等；输出 32×32×3 ∈ [−1,1]；参数量 = torch 解析值；BN eps = 1e-5；生成器实际输入 ∈ [0,1]。**A4 已实现（c54a2092）**：开关 `backdoor.badpfl_generator: official`（`build_autoencoder_official`）；L1 `tests/test_badpfl_official.py`（参数量 346,659、SAME ≡ torch p1） |
| A15 | 确定性 | `utils.py:8-13` 只播 torch + `cudnn.deterministic=True`；**numpy 与 python `random` 没播** → 划分（`main.py:67`、`utils.py:65`）与客户端顺序（`main.py:95`）每次运行都不同，官方自身不可复现（FINDINGS F-024） | 三处随机源都播了（`main.py:325-339`）；GPU 算子未设确定性；tf.data 并行 `map` 里有带状态的随机增强（`partition.py:40`）。实测同 seed 重跑第 1 轮即分叉（F-002） | `done` | **D-028**：`tf.config.experimental.enable_op_determinism()`（为内部效度，比官方更严）。F-002 的分叉来自 GPU kernel 还是增强，**没有证据**。若容器 TF 版本有算子不支持（抛 `UnimplementedError`）或慢得不可接受，回审计重议 | L1：AST —— `run_experiment` 在建模型之前调用它；集群：加一条 `[Checksum]` 自描述行（每轮全局权重 sha256 前 12 位），同 seed 两次 run 前 5 轮哈希相同。**A4 已实现（7e43a426）**：开关 `training.deterministic_ops: true` + `[Checksum]` 行；L1 `tests/test_schedule_alignment.py`（AST、checksum）；TF 2.15.1 CPU 上同配置两次 run 的 checksum 逐轮相同。**pilot 第一轮（`e2ee9ca`）在 GPU 上全崩**：推理模式 BN 求梯度没有确定性 kernel（F-043）→ **D-043**（D-028 重议）：保留确定性，`resnet10_torch` 的 BN 推理模式不走 fused 核；L1 `tests/test_bn_inference_determinism.py`。良性路径第 1 轮跨作业相同（F-044，部分证据）。**待 DET 组重跑（GPU）后改 `done`**。**pilot（`2853433`）DET `pass` → `done`**：DET rep1 / rep2（作业 3043736 / 3043737，节点 n553 / n137）前 5 轮 `[Checksum]` 逐轮相同（`d259128657fd` … `931d1867fbac`），恶意端与评估路径都在（F-045） |
| A16 | 死配置 | 无 label smoothing | `training.label_smoothing: 0.1` 在配置里，代码不读（A2 会话 grep 复核：`fedavg/` 中 0 处） | `done` | **D-028**：从 P2 配置删除；冻结的旧配置不动（D-003） | L1：P2 配置不含 `label_smoothing`；`fedavg/` 中读取它的地方 = 0（将来谁实现了它，这条先红，旧配置里的 0.1 不会悄悄生效）。**A4 已实现**：P2 基配置 `base.yaml` 不含 `label_smoothing`；L1 `tests/test_registry.py::test_label_smoothing_is_read_nowhere_in_fedavg` 与 `::test_v2_base_is_decided_in_a4_and_carries_the_p2_template` |
| A22 | 目标标签 | 论文 p.7「The target label y_t is randomly generated」；官方代码 `main.py:36` 默认 `--ba_target_label 0` | 所有配置 `backdoor.target_label: 0` | `done` | **D-028**（用户拍板）：固定 0，= 官方代码、偏离论文。理由：3-B 的 C1–C4 围绕 y_t 的占比设计；P1 的噪声估计都在 y_t=0；与 Bad-PFL 库可同框。代价：结论只针对 class 0（airplane） | L1：P2 基配置 `target_label == 0`。**A4 已实现**：`base.yaml` 的 `target_label: 0`；L1 同 A16 那条 |
| A23 | 训练轮数 | 论文 p.7：1000 轮；p.18：约 500 轮时模型与攻击都已收敛；官方代码 `main.py:23` 默认 `--total_round 300` | 标定后：有效轮 floor 150 / cap 300 | `deviate` | **D-028**：保留 floor 150 / cap 300。按 A09 的换算，floor 150 ≈ 每端 75 epoch，已超过论文收敛点（500 轮 ≈ 50 epoch）。A08 改成按有效轮衰减后 HFL 格子的 lr 变低，收敛是否受影响由 D-029 的 `stop_reason` 与 pm_acc 判据复核 | 集群：D-029 |
| A24 | FedRep 哪个阶段投毒 | 官方实际执行的只有 FedBN 单模型（`main.py:6,89-94` 只用 `BasicClient` / `PoisonClient`），**没有对应物**；`client.py:86-137` 的 `PMClient` / `PMPoisonClient`（两步都投毒）是死代码，从未被 import（FINDINGS F-022）。论文 Alg.1（p.5）：第 11 行恶意端按 Eq.4 训 F(·;θg)；第 15 行「Train the personalized model with the predefined PFL method」在 `end` 之后，良性端与恶意端共用，未提投毒 | `client/hier_fedrep.py:139-150`：head 阶段（`:141`）与 body 阶段（`:148`）都经过 `on_batch` → 都投毒（`client_badpfl.py:25-27` 的「决策 B」） | `done` | **D-021：按论文 Alg.1 字面，只在 body 阶段投毒**，head 阶段用干净数据。只改 `hier_fedrep`（其他方法的决策 B 不在实验 3 范围）。影响 3.2：head 不再直接看到投毒样本（FINDINGS N-003）。训练顺序（Alg.1 先 local 后 personalized；TF 先 head 后 body）→ A26（A3：两种都跑 pilot，D-031） | L1：FedRep 恶意端一次 `local_train` 中，head 阶段 0 个 batch 被投毒，body 阶段每个 batch 都经过投毒分支。**A4 已实现（943bbf90）**：开关 `training.fedrep_poison_phases: body`；L1 `tests/test_fedrep_resnet_alignment.py`（事件序列：head 阶段 0 个投毒批） |
| A25 | 每 epoch 的取数 | `main.py:78-80` `SubsetRandomSampler` + `drop_last=True`，`client.py:48-57` 持久迭代器：**每 epoch 重洗**，丢掉的尾批每 epoch 不同 | `hier_fedrep.py:65` 构造时 `list(self.dataset)` 缓存一次 → 增强冻结（F-014）、**batch 组成冻结**，`:173-177` 只打乱 batch 顺序并永远丢同一个尾批 → seed42 下 1453/45037 = 3.2% 的训练样本从未参与训练，单端最高 19.5%（F-025）。FedAvg 客户端（`client_fedavg.py:44`）每 epoch 重读 tf.data（重洗、重增强、含尾批）→ G4 混入数据流差异（F-026）；生成器（`client_badpfl.py:100`）另读一遍 tf.data、循环取批 | `done` | **D-026**：修复 —— 统一为官方语义：每 epoch 重洗 + 重增强、`drop_last` 且尾批每 epoch 轮换；FedAvg、FedRep、生成器走同一个取数函数。做成配置开关（默认 = 现行为），并入 D-029 可行性实验 | L1：同一客户端连续两个 epoch 的 batch 组成不同、被丢的样本不同（反向锚点：现缓存下相同）；每 epoch 覆盖 n − (n mod 32) 个不重复样本；AST：三处取数调用同一函数；集群：D-029。**A4 已实现（943bbf90）**：开关 `data.batch_pipeline: per_epoch`（`data/epoch_pipeline.py` + `FLClientBase.epoch_batches`）；L1 `tests/test_epoch_pipeline.py`。**待 D-029 通过后改 `done`**。**pilot（`2853433`）D-029 `pass` → `done`**（F-045） |
| A26 | FedRep 训练顺序 | 官方执行代码里没有 FedRep（F-022）。论文 Alg.1（p.5）：第 11 行先训 F(·;θg)，第 15 行「Train the personalized model with the predefined PFL method」→ 字面是先训共享部分、后个性化。FedRep 原作者 `Update.py:599-613`：先 head（`iter < head_eps`）后 representation；PFLlib `clientrep.py:32-37` 先 head、`:56-61` 再 base。官方死代码 `client.py:94` `update_order="global_personalized"` 不作证据（F-022） | `client/hier_fedrep.py:138-151`：先 head（`plocal_epochs`）后 body（`local_epochs`） | `deviate` | **D-031（用户：「对两个方法进行测试，看看效果是否相同」）**：A4 加开关 `training.fedrep_order: head_first｜body_first`（默认 head_first = 现行为）。body_first = 先训 body（按 D-021 投毒；head 冻结在该端上次的私有 head），再在刚训完的 body w_k 上用干净数据训 head；它的陈旧 PM = [w_k, h]，fresh-PM 定义不变（A28）。pilot：flat_baseline + 2edge_distributed、seed42 各 1；head_first 臂 = D-029 的两个 run，另加 2 个 body_first run（约 2.5 GPU-h），登记在 D-029 的 pilot 登记表（P1 口径，不进 P2）。**预注册判据「效果相同」**：两格都满足 fresh-PM pm_acc 末 10 点均值之差的绝对值 ≤ 0.006，且 local_benign_asr 末 10 点均值之差的绝对值 ≤ 0.07（= P1 同 seed 重复的散布：pm_acc 见 D-029，ASR 取 flat 的 0.685–0.755）。相同 → 维持 head_first（FedRep 原顺序），本行改 `deviate`（偏离 Alg.1 字面）；不同 → 带回审计由用户定。单 seed 下只分辨得出大差异（ASR 约 0.07） | L1：head_first 时 head 阶段的 batch 全部在 body 阶段之前，body_first 时相反（调用记录）；两种顺序下都只有 body 阶段投毒（并入 D-021 的 L1）；集群：pilot。**A4 已实现开关（943bbf90）**：`training.fedrep_order: body_first`；L1 `tests/test_fedrep_resnet_alignment.py`。pilot 已登记（`pilot/registry.yaml` 的 A26 组），按 D-031 判定（`harness/pilot_a4.py`）。**pilot（`2853433`）D-031 判 `different`**：fresh pm_acc 差 0.097 / 0.104（门槛 0.006），local_benign_asr 差 0.137 / 0.077（门槛 0.07；两格方向相反）。**D-045（用户）：维持 head_first → `deviate`**（偏离 Alg.1 字面），不改代码（F-045） |
| A27 | FedRep 下 BN 的归属 | 官方只跑 FedBN：`pfl.py:3-24` 把键名含 `bn` 或 `shortcut.1` 的一律不上传、不下发 → γ/β 与 running 统计量全在本地（`server.py:4-10` `agg_avg` 本身平均的是整个 state_dict）。PFLlib FedRep：只聚合 `.parameters()`（`serverbase.py:138-150`），下发只覆盖 parameters（`clientrep.py:86-88`）→ γ/β 聚合、running 统计量（buffer）留在本地。FedRep 原作者的 CNN 没有 BN（`Nets.py:62-87`）。HierFAVG 聚合 state_dict（`client.py:73`、`average.py:5-14`）→ 统计量也聚合 | `models/cnn.py:201-202` + `get_base_head_indices`（`:238` 起）：γ/β 与 moving_mean/variance 都算 body，随 `get_weights` 一起聚合、一起下发 | `done` | **D-032：γ/β 共享，moving 统计量私有**（SiloBN 式，= PFLlib 的实际行为）。**这是实现选择，不是框架差别**：两个框架都能二选一（torch `parameters()` 对 `state_dict()`，Keras `trainable_weights` 对 `get_weights()`）；FedRep 论文的模型没有 BN，没有权威答案（F-036）。理由（**均为机制推断，无数值证据**）：① 10 edge 时每 edge 轮只有 1 个参与者（N-001），edge 的统计量约等于上一个参与者的本地统计量，flat 则是 10 端的混合 → PM 归一化的失配随拓扑变化，而拓扑正是 3-A 的自变量；② 官方 PM 用本地统计量归一化（FedBN）。P1 末 10 点 pm_acc：flat 0.743–0.745、2edge 0.736–0.745、10edge 0.732–0.740，差 ≤ 0.013，既证明不了、也排除不了这个效应。实现：客户端像对待 head 一样保留自己的统计量（首次接收时采用广播值）；PM 的统计量取 **head 阶段结束时的快照**（此时 body 冻结在 φ_e）；edge / cloud 照旧聚合统计量，只供 EM / GM 评估，不下发覆盖客户端。与 A29 联动：统计量私有时要用 torch 的 momentum（Keras 0.9），否则 head 阶段约 14 步后仍有 0.99^14 ≈ 87% 是上次参与时的旧值 | L1（TF）：两个客户端的统计量设成不同哨兵值，`set_weights` 后统计量 = 自己的、γ/β = edge 的、head = 自己的；首次接收时 = 广播值；PM 的统计量 = head 阶段末的快照；反向锚点：现代码下统计量被 edge 覆盖。**A4 已实现（943bbf90）**：开关 `training.fedrep_bn_stats: private`；L1 `tests/test_fedrep_resnet_alignment.py`（哨兵值；shared 下被 edge 覆盖为反向锚点）。并入 D-029：没通过要回来重审 |
| A28 | 评估用的个性化模型（PM），以及 ASR 与 acc 同模型 | 官方（FedBN）：训练结束时对全部客户端评估 `client.local_model`（`main.py:128-131`），即每端最后一次参与、本地训练**之后**的模型（陈旧），ASR 与 acc 在同一模型上。FedRep 原作者：每轮评估 `w_locals`（`main_fedrep.py:167,174`、`test.py:108-113`）= 最后一次参与后的 [w_k, h]；最后一轮所有端在最终 φ 上把 head 训 ≥ 10 epoch（`main_fedrep.py:129-130`、`Update.py:575-600`）。PFLlib：每轮先把 base 下发给**全部**客户端再评估（`serverrep.py:26-32`、`serverbase.py:103-109`）→ [当前 φ, 上次的 h, 本地统计量] | P1：ASR（`backdoor_eval.py:257`）与 pm_acc（`server.py` → `client.evaluate_on`）都在 `client.model` 上 = [上次参与时收到的 φ_e, 在它上面训的 h, φ_e 的聚合统计量]（`hier_fedrep.py:160-163`）。四个参考各不相同，这个定义只有本仓库有（F-035） | `done` | **D-033：硬规则（用户提出）：主 ASR 与主 pm_acc 在同一个个性化模型上测。** 两者都用 **fresh-PM** = [评估时刻所在 edge 的当前 body, 自己的 head, 自己的统计量]（= PFLlib；统计量私有见 A27）。P1 的陈旧 PM 上的 ASR 与 pm_acc 一起留作副列。攻击者算 ξ 的模型 = D-015 选中的那个攻击者**自己的 fresh-PM**（修订 D-015 的「最后一次训练后的陈旧模型」，让受害者与攻击者同一定义，对应官方两边都用 `local_model` 的对称性）。连带：停轮判据 `pm_acc_plateau` 改读 fresh-PM 的 pm_acc（参数在 P1 陈旧 pm_acc 上标定，D06 已附 D-029 复核）；D-029 的 pm_acc 门槛仍按陈旧列判（与 P1 可比）。fresh-PM 的实现从 S5 挪到 A4，S5 只剩逐 edge 轮评估 | L1：不依赖 TF 的 `compose_pm(edge_w, private_w, private_idx)` 逐元素断言；主 ASR 与主 pm_acc 取自同一个组装好的模型（调用记录断言）；攻击者 ξ 模型 = 其 fresh-PM；陈旧列仍是 `client.model`。**A4 已实现（d08bdea6）**：开关 `evaluation.pm_model: fresh`（`utils/pm.py:compose_pm`、`CloudServer.main_pm`）；陈旧列 `[Stale]` / `[StaleASR]`（ξ 用攻击者自己的陈旧模型，A4 用户拍板）；L1 `tests/test_eval_integration.py`（逐客户端：主 pm_acc 与主 ASR 的模型哈希 = compose_pm 的哈希） |
| A29 | ResNet-10 结构（A17 的结论） | `resnet.py:15-40,74-114`，`main.py:89,103` 用 `get_resnet(size=10)`。stride-2 的 3×3 卷积 `padding=1`（`:20-21`，两侧对称）；BN 用 torch 默认 momentum 0.1、eps 1e-5；初始化用 torch 默认（卷积 kaiming_uniform(a=√5)，方差 1/(3·fan_in)；Linear 的 bias ~ U(±1/√fan_in)）；`avg_pool2d(out,4)` + Linear 输出 logits | `models/cnn.py:172-218` `build_resnet10`：stride-2 卷积 `padding="same"`（`:182`）；Keras 默认 BN（momentum 0.99、eps 1e-3）；glorot_uniform、bias 为 0；GAP + Dense softmax | `done` | **D-034：三项都对齐**，放进新 arch 名 `resnet10_torch`，冻结的 `resnet10` 不动（D-003）：① stride-2 的 3×3 卷积改为 `ZeroPadding2D(1)` + `valid`；② BN momentum 0.9（= torch 0.1）、eps 1e-5；③ 初始化 `VarianceScaling(1/3, "fan_in", "uniform")`，Dense 的 bias ~ U(±1/√fan_in)。**三处都起因于框架约定或默认值（陷阱 #6），但 TF 都能逐项复现，属移植对齐问题，不是不可消除的框架差异**。数值证据（F-033）：trainable 4,903,242 与 torch 解析值相等；`same` 下主路采样奇数行、1×1 shortcut 采样偶数行，残差相加错位 1 像素（3×3/s2 的输出最大差 32.2）；Keras 自带的 ResNet 也在 stride-2 卷积前显式 `ZeroPadding2D`（`keras/src/applications/resnet.py:356,441`）。等价、不改的项：stem（s1 时 same ≡ p1）、结构 [1,1,1,1] 与通道、1×1 shortcut（pad 0）、GAP ≡ `avg_pool2d(4)`（32×32 输入）、softmax + `from_logits=False`（输入梯度与 logits-CE 逐元素相等）。并入 D-029（D-038） | L1（TF）：trainable = 4,903,242、统计量 = 5,760；中心抽头的 stride-2 卷积采样偶数行（反向锚点：旧实现奇数行），主路与 shortcut 采样位置相同；12 个 BN 层 momentum 0.9、eps 1e-5；大层 kernel 的经验方差 ≈ 1/(3·fan_in)（±5%），Dense bias ∈ ±1/√512 且不全为 0；旧 `resnet10` 的层配置不变。**A4 已实现（943bbf90）**：`model.arch: resnet10_torch`；L1 `tests/test_fedrep_resnet_alignment.py`（4,903,242 / 5,760、偶数行采样、BN 0.9 / 1e-5、初始化方差）。并入 D-029：没通过要回来重审 |

## 二、还没查的（每一项都是 `open`，查完才能关；A3 会话已全部查完）

| ID | 项 | 需要什么 / 查到了什么 | 状态 | 去向 |
|---|---|---|---|---|
| A17 | ResNet-10 逐层对比（`resnet.py` 152 行 vs TF）。**更正：TF 的实现在 `models/cnn.py:172-218`，不在 `models/resnet.py`**（后者是 GroupNorm 版 ResNet-56 / WRN / DenseNet，exp3 没用到，F-034） | A3 会话读完 `resnet.py`，并在 TF 2.15.1 上跑数值探针（F-033）：结构与参数量一致；三处差异（stride-2 的 padding、BN momentum / eps、初始化） | `done` | → A29（D-034） |
| A18 | `trigger.py`、`event_emitter.py` 是否影响攻击/评估流程 | A3 复核 F-032：11 个官方文件里 `fl_event_emitter` 没有任何 `.on(` 或装饰器注册，`fl_process.py` 的 6 处 `emit` 全是空操作；`grid_trigger_adder` 只在 `main.py:11` 被 import、从未调用；执行路径上的钩子是另一套 `register_func`（`fba.py:62`、`pfl.py:23-24`），A1 / A2 已覆盖。残余：官方仓库的文件列表取自 A2（本会话列不了目录） | `done` | 不影响攻击 / 评估流程（D-035、F-040） |
| A19 | FedRep 参考实现 | 已定：**以 Bad-PFL 论文附录 A 为准**（D-012）；原作者与 PFLlib 已读，差异记在 F-016 | `done` | 参考源问题已结，实现差异转到 A12 |
| A20 | 论文正文超参 | A2 会话按原文复核（p.7 §4.1、p.8、p.13–14 附录 A、p.18）：100 客户端、**1000 轮**（p.18：约 500 轮收敛）、10 个恶意端、每轮 10%、Dirichlet 0.5、SGD lr 0.1、batch 32、15 步、投毒率 0.2、ε=σ=4/255、生成器 Adam 0.01 × 30 步、**目标标签随机生成**、FedAvg 聚合时各客户端等权（p.8）；MultiKrum f=1 选 5 个。p.7 注明 FL 设定「Following existing studies (Zhuang et al., 2024)」—— 训练超参是继承来的，不是攻击的一部分（D-022）。去向：100 客户端 / 10 恶意 / 每轮 10% / batch 32 / 投毒率数值 / 生成器 Adam 0.01×30 步 = 一致；Dirichlet → A11；1000 轮 → A09、A23；lr → A08；15 步 → A09；等权 → A07；目标标签 → A22；ε=σ（配置 0.01569，与 4/255 差 0.02%）→ A01、A10；投毒取整 → A03；ResNet-10 → A17；MultiKrum → 不在实验 3 范围 | `done` | 已逐项分派到各 A 行（A2 会话，F-031） |
| A21 | HFL 的形式化（Liu et al. HierFAVG：edge/cloud 聚合权重、κ₁/κ₂） | A3 读 HierFAVG 官方代码（论文原文读不到）：edge 按本 edge 轮**参与者**样本数加权（`hierfavg.py:307` 每轮 refresh，`edge.py:46-48,55-63`）= TF `aggregation/fedavg.py:17-27`；cloud 按 edge **全体成员**样本数加权（`cloud.py:25-27,34-37`）= TF `edge_server_base.py:36`；κ₂ = `edge_rounds`；横轴 = 有效轮（`hierfavg.py:334,337`）。偏离（按用户拍板记 `deviate`，偏离项直接写在本行）：① κ₁ 以 epoch 计、随客户端大小变（官方每端同为 `num_local_update` 步；S3 等大小后每端相同）；② edge 内均匀抽样（官方按样本数成比例抽，`:309-313`；S3 后相同）；③ 参与配额 → D02。执行顺序（官方交错，`:299-325`）→ D01 对齐 | `deviate` | 一致项与偏离理由见 D-035、F-037 |

## 三、有意偏离登记（HFL 特有，官方没有对应物）

这些都是设计选择，**要你签字**才算关闭。签字前一律 `open`。
A3 会话（2026-09-26）：D01 / D02 决定修改后关闭（`align`，A4 实现并配 L1 后改 `done`，D-036）；D03–D06 签字（`deviate`，D-037）。

| ID | 项 | 现状（出处） | 理由 / 决定 | 代价 | 状态 |
|---|---|---|---|---|---|
| D01 | edge 层与执行顺序 | `server/server.py:208-230` `collect_and_aggregate`：每个 edge 连续跑完 `edge_rounds` 轮再轮到下一个（`server/hier_fedrep.py:77-89` `run`） | 研究对象本身。**D-036：改为按 edge 轮交错执行**（HierFAVG 官方 `hierfavg.py:299-325`：外层 edge 轮、内层 edge）。理由：`badpfl_shared_generator: true`（`main.py:491-499,529-531`，对齐官方 `fba.py:27`）让所有恶意端共用一个生成器；顺序执行时 E1 的攻击者拿到的生成器已在 E0 第 R 轮的模型上训过，云轮末评估也总用刚在最后一个 edge 上训完的生成器（F-038）。**不违反共享生成器**：仍是同一个对象，只改更新的先后次序。L1（不依赖 TF）：调度循环抽出后，调用顺序 = [(t, e) for t in 1..R for e in edges]；各 edge 的 rng 选出的客户端与顺序执行时相同；共享生成器仍是同一对象。**A4 已实现（7e43a426）**：开关 `federation.edge_schedule: interleaved`；L1 `tests/test_schedule_alignment.py` | 要把 edge 的 `run()` 拆成由 cloud 驱动的逐 edge 轮；纯良性训练统计上等价、不逐字节相同（全局 Python `random` 的消耗顺序变）；只影响恶意端分布在 2 个以上 edge 的格子；顺序偏差的大小无数值证据 | `done` |
| D02 | 参与配额 | `server/participation.py:44-52` + `edge_server_base.py:103-117`：全局预算 `B=max(n_edges, round(N·frac))`，整数配额按 **`global_round_idx`（云轮）** 轮转，每 edge ≥1 | 原理由保留：不同拓扑下每有效轮训练的客户端数相同；恶意端期望参与次数相同（collocated 4edge：2.5×10/25=1.0；distributed 10edge：1×1/10×10=1.0）；不空转；n_edges=1 时退化成官方「100 抽 10」。**D-036：轮转改按有效轮 t_eff**。实测（F-039）：4 edge、B=10 时一个云周期内各 edge 的参与次数 R=5 为 15/10/15/10 → 13/12/13/12，R=10 为 30/20/30/20 → 25/25/25/25，R=20 为 60/40/60/40 → 50×4；1、2、10 个 edge 时每轮配额向量恒定 → 逐元素不变。G1 正是 4 edge × R {10, 20}。L1（`participation.py`）：R=1 时与旧序列相同；每 edge 轮配额和 = B；E=4、R=10 时一个云周期内各 edge 总名额差 ≤ 1（反向锚点：旧公式 30/20）。**A4 已实现（7e43a426）**：开关 `federation.quota_round_axis: effective`；L1 `tests/test_schedule_alignment.py`（4 edge、R=10：25×4，旧 30/20 为反向锚点） | **10 edge 时每 edge 每轮只有 1 个客户端** → edge 内聚合退化成复制（N-001）；n_edges>B 时 `max` 悄悄抬高预算；4 edge 格子的客户端抽样序列随之改变 | `done` |
| D03 | 按 edge 布点 | `malicious_placement: by_edge`（`attack/backdoor.py:97-162`，要求 `edge_assignment: block`） | 3-B/3-C 需要控制「哪个 edge 有攻击者」。**D-037 签字** | P1 客户端不等大 → 名义 10% 的恶意端实际数据占比随布点在 0.092–0.142 之间（F-028），S3 等大小后消失（D-027）；smoke 的 `spread` 在 random 分配下不跨 edge（F-023，只影响 smoke） | `deviate` |
| D04 | 探针 = 客户端留出分片 | 陷阱 #11；`backdoor_eval.py:188-343`；`per_client_test_ratio: 0.25`（`data/partition.py:45-62`） | 与 pm_acc 同一 population；PFLlib 口径。官方从官方测试集按同一类先验每端抽 100 张（`main.py:66,73-77`），两边都是「该端自己的分布」。**D-037 签字** | 与官方「官方 test 独立划分」不同（A11）；P1 下每端探针大小随客户端大小变，S3 等大小后相同 | `deviate` |
| D05 | 逐 edge 指标 | `per_edge_*`（`[Acc]` 行、`per_edge_acc_*`、backdoor 的逐 edge 字段） | 3-B/3-C 的判据是逐 edge 的；纯增量输出，不改训练。**D-037 签字** | 附带：总体 pm_acc 按样本加权（`server.py` `run_round`），官方 Avg acc 等权（`main.py:137`）；S3 等大小后数值相同 | `deviate` |
| D06 | 自适应停轮 | `server/stopping.py`（有效轮 floor 150 / cap 300） | 跨拓扑在相同完成状态下读数。**D-037 签字** | 各格长度不同（靠 floor 保证同轮比较点，cap 处的截断单独报）；判据参数在 P1 口径下标定，A4 后由 D-029 复核；A28 之后 `pm_acc_plateau` 读的是 fresh-PM 的 pm_acc | `deviate` |

## 四、流程

> **术语**：A1–A4 是**会话名**（PLAN §5），A01–A29 与 D01–D06 是本表的**行号**，两者无关；DECISIONS 的 D-001… 带连字符、三位数，与登记行 D01–D06 也是两套东西。A4 = 按拍板改代码的实现会话（与行 A04「生成器训练数据」撞名，没有改名，因为 `harness/registry.py` 的报错文案和两条测试引用了「A4」）。

1. **A1**（攻击：A01–A06、A14、A24 —— 2026-09-25 已拍板）→ **A2**（训练协议：A07–A11、A13、A15、A16、A20、A22、A23、A25 —— 2026-09-25 已拍板，原则见 DECISIONS D-022；A08 待 D-029 可行性实验）→ **A3**（FedRep / ResNet / HFL：A12 的 head 层、A17、A18、A21，新增 A26–A29，D01–D06 —— 2026-09-25/26 已拍板，D-030 … D-038；A26 待 pilot）。
2. 每个审计会话按 CLAUDE.md 的格式出语义 diff 表：论文公式 | 官方实现 | 本仓库实现 | 差异 | 怎么验证。你**逐行**拍板，结论写回本表的「状态」和「决定」两列，并在 DECISIONS.md 记一条。
3. **A4** 按拍板改代码，每个 `align` 配 L1 测试 → 状态改 `done`；跑 D-029 可行性实验（D-038 扩大后：A08 + A25 + A27 + A29，2edge 那格带上 D01 / D02），通过后 A08 改 `deviate`；同时跑 A26 的 body_first 对照（+2 个 run），按预注册判据关 A26；口径版本升 P2（`fedavg/utils/provenance.py`）；2 个 smoke run 复核标定。
4. 本表全部关闭 → `status` 解除 blocked → 开跑 P2。

**A4 会话（2026-09-26）进度**：全部 `align` 行已按拍板实现为开关（默认 = 旧行为），对齐项收成一套模板 `fedavg/config/alignment_p2.yaml`（DECISIONS D-039）。L1 通过的行改 `done`：A01 A02 A03 A05 A06 A14 A16 A22 A24 A27 A28 A29 D01 D02。仍未关：A15（等 pilot 的 GPU 确定性对）、A25（等 D-029）、A08（D-029 通过后 `deviate`）、A26（按 D-031 判）。pilot 登记在 `pilot/registry.yaml`（6 个 run），用 `pilot/submit_pilot.sh` 提交、`harness/pilot_a4.py` 判定。

**A4 收口（2026-09-26）**：pilot 第一轮 6 个 run 全部 `invalid`（GPU 确定性 × 推理模式 BN 梯度，F-043），四行都没关。D-043 修复、D-044 给判定加有效性闸；重跑先交 DET 组。

**A4 收口第二轮（2026-09-27，`2853433`）**：6 个 run 全部有效。D-029 `pass`（A08 → `deviate`、A25 → `done`）；DET `pass`（A15 → `done`）；D-031 `different` → 用户定维持 head_first（D-045，A26 → `deviate`）。**AUDIT 全部关闭**，口径升 P2（`fedavg/utils/provenance.py`）。标定复核由 pilot 的 D029 两格抵扣（D-046）。
