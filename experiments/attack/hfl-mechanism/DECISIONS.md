# 决策日志 —— Experiment 3（改版）

> 每条：编号 | 日期 | 决定 | 依据 | 状态。**只追加，不改写**；推翻旧决定时新开一条，并在旧条目的「状态」里注明被哪条取代。
> 决定来自用户本人，或经用户确认。Claude 的建议不算决定。

| ID | 日期 | 决定 | 依据 | 状态 |
|---|---|---|---|---|
| D-001 | 2026-09-24 | S1 做「结果管理 + 分析底座」，不改训练/评估行为 | 用户选择（优先解决结果与文档管理） | 生效 |
| D-002 | 2026-09-24 | 改版实验 3 的全部实验（含 3.1–3.3）都在 tf-dpfl 跑，不在 torch Bad-PFL 库跑 | 下限必须和它约束的 ASR 用同一套评估管线 | 生效 |
| D-003 | 2026-09-24 | 新方案放 `experiments/attack/hfl-mechanism/`；`hfl-propagation/` 冻结为旧方案，只加横幅，不移动文件 | 有 10 个测试依赖旧路径 | 生效 |
| D-004 | 2026-09-24 | 评估时 ξ 对齐官方：主 ASR 在**攻击者**模型上算 ξ；白盒版本（ξ 算在受害者模型上）另记一列副指标，作上界 | AUDIT A02：官方 `fba.py:53,64`，TF `client_badpfl.py:164-172` | 生效（A4 实现） |
| D-005 | 2026-09-24 | LR 改为按有效轮衰减 | 当时只知道 LR 按云轮衰减会混淆 flat 与 HFL | **已作废** → 见 D-010 |
| D-006 | 2026-09-24 | **`AUDIT.md` 全部关闭之前，不跑任何 P2 run** | 用户：「确定以后没有这些和原代码、原文献没有对齐的细节再开始实验」 | 生效 |
| D-007 | 2026-09-24 | `exp3_cell.sbatch` 写死 `--defense none` 的问题本会话只记录、不修 | 用户：「一件一件做，先记住」。已记入 FINDINGS F-001 与 CLAUDE.md 陷阱 #19 | 生效 |
| D-008 | 2026-09-24 | 本计划只覆盖改版实验 3；原文 §9 阶段三与 §11 算力方案不在范围内 | 用户：「只需要做完改版的实验 3」「忽略 §11」 | 生效 |
| D-009 | 2026-09-24 | P1 批次（`d8c8d0b`，seed42）定为试点：不进入结论，只用于噪声/效应量估计、提出假设、工具测试 | 它带着 AUDIT A01–A12 的全部差异，且只有 1 个 seed | 生效 |
| D-010 | 2026-09-24 | LR 日程**重新决定**，放到 A2 审计会话 | 官方 `main.py:57` 是常数 lr=0.1，没有调度器（AUDIT A08） | **已决** → 见 D-023（A2 会话） |
| D-011 | 2026-09-24 | 口径版本的名字用 P0/P1/P2，不叫 epoch | 避免与训练的 `local_epochs` 混淆 | 生效 |
| D-012 | 2026-09-25 | **FedRep 与 Bad-PFL 论文保持一致**：head、body 各 SGD lr=0.1、batch 32、15 步，不单独设 head lr。原作者实现（Collins）与 PFLlib 只作旁证 | 论文 p.13 附录 A：「we use the same training configuration as that of the local models to train personalized models」；三种实现的差异见 FINDINGS F-016 | **已被 D-024 取代**（2026-09-25 A2 会话：训练协议保留调过参的现状）。「head 是哪几层、BN 算不算 body」仍在 A3 定 |
| D-013 | 2026-09-25 | Bad-PFL 论文 PDF 不入库（10.5 MB，超过单文件 10 MB 红线）；引用一律写「论文 p.N」 | CLAUDE.md 红线 | 生效 |
| D-014 | 2026-09-25 | **A01：ξ 对齐官方代码**（单步 PGD）：U(−σ,σ) 随机起点 → clamp 合法像素范围 → 在起点处求梯度 → +σ·sign → 投影 ±σ → clamp；+δ 之后不 clamp。生成器训练 / 训练期投毒 / 评估三处都用。论文 Eq.6 的 FGSM 不采纳 | 官方 `fba.py:6-22,55`；论文 p.6 与代码不一致，用户选代码（FINDINGS F-019） | 生效（A4 实现） |
| D-015 | 2026-09-25 | **A02：主 ASR 的 ξ 用一个固定的恶意端**：setup 时按 seed（`default_rng([seed, …])`）从恶意端里选 1 个、全程不变，其 id 与所在 edge 写进 `[设定]` 行；用它最后一次训练后的模型。白盒列照 D-004 保留 | 官方 `fba.py:60-64` + `main.py:95`：shuffle 后最后一个恶意端 ≈ 开跑时随机选一个、全程固定 | 生效（A4 实现） |
| D-016 | 2026-09-25 | **A03：投毒量改为逐样本伯努利(ρ)**，用客户端自己的 seeded rng；一个都没抽中就原样返回 | 官方 `fba.py:48-50`；现实现的取整偏差见 FINDINGS F-020 | 生效（A4 实现） |
| D-017 | 2026-09-25 | **A04：生成器用干净数据训练（对齐论文 Eq.7），偏离官方代码** —— 即保持现状 | 论文 p.6 Eq.7 明确用干净 (x, y)；官方 `fba.py:36` 变量名 `clean_data`，却被 `client.py:124-125` 覆写成投毒数据，疑似 bug。用户选论文（FINDINGS F-019）。代价：F-013 的 ρ=1 抵消不复现 | 生效（AUDIT A04 = `deviate`，无需改代码） |
| D-018 | 2026-09-25 | **A05：BN 模式**：投毒时与评估时生成 δ，生成器用 batch 统计（评估按 32 张分块）；训练期求 ξ 时 F 用训练模式；训练生成器的两处已一致；**评估时攻击者的 F 用推理模式（偏离官方）** | 官方 `fba.py:32`、`client.py:35`、`utils.py:33`，生成器从不 `.eval()`；官方评估时 ξ 模型的模式取决于客户端列表顺序，是偶然行为，不复现。现状的生成器 BN 失配见 FINDINGS F-021 | 生效（A4 实现） |
| D-019 | 2026-09-25 | **A06：ASR 四列都报**（过滤 / 不过滤 × 全体 / 仅良性，逐客户端等权平均）；**主指标 = 过滤 × 仅良性**（= `local_benign_asr`）；「不过滤 × 全体」作为与论文可比的列 | 官方 `main.py:127-134` + `utils.py:29-48` 是不过滤 × 全体；3-B 刻意改变各 edge 的 y_t 占比，不过滤的口径会被占比机械抬高 | 生效（A4 实现另外三列） |
| D-020 | 2026-09-25 | **A14：生成器结构以官方代码为准**：保留 `padding="same"`（用 L1 证明与 p1 等价）、加 bias、BN eps 1e-5、init 用 `VarianceScaling(1/3, "fan_in", "uniform")`（= torch 默认）、生成器输入先反标准化到 [0,1]、末层不加 BN | 官方 `generator.py:6-40`；论文 p.14 表 5 末层写了 BN，与代码不一致，用户选代码（FINDINGS F-019） | 生效（A4 实现） |
| D-021 | 2026-09-25 | **A24：FedRep 只在 body 阶段投毒**，head 阶段用干净数据（按论文 Alg.1 字面）。只改 `hier_fedrep`，其他方法的「决策 B」不在实验 3 范围内 | 论文 p.5 Alg.1 第 11 行（恶意端按 Eq.4 训 F(·;θg)）与第 15 行（个性化训练在 `end` 之后，良性/恶意共用）。官方实际执行的代码没有对应物：`PMPoisonClient` 是死代码（FINDINGS F-022）。对 3.2 的影响见 FINDINGS N-003；训练顺序在 A3 定 | 生效（A4 实现） |
| D-022 | 2026-09-25 | **训练协议的对齐原则**：攻击定义（A1 那几行）必须对齐；训练协议（A2 那几行）不以「与官方一样」为目标，判据为 ① 被比较的因素之间无混淆 ② 修 bug ③ 与论文量级可比（总训练量、收敛、干净精度）④ 改动调过参的值，须先过可行性实验 ⑤ 不改的记 `deviate` 并写理由 | 用户：「对于这一类训练超参数真的应该完全匹配吗？毕竟我和 bad-pfl 的代码是在不同的联邦学习结构上运行的」「如果修改需要经过一组实验证明可行性」。论文 p.7：FL 设定「Following existing studies (Zhuang et al., 2024)」 | 生效 |
| D-023 | 2026-09-25 | **A08：LR 保留调过参的 0.992 与 head 0.005，改为按有效轮衰减**：`0.1·0.992^t_eff`、head `0.005·0.992^t_eff`，t_eff = 截至本 edge 轮已完成的有效轮数（R_edge=1 时就是现在的云轮号 → flat 逐字节不变）。配置开关，默认 = 现行为 | F-003 的 flat/HFL 混淆（同一有效轮上 lr 不同）；两个值的来历见 F-030；用户在「按有效轮衰减 / 保持现状并记为已知混淆 / 常数 0.1」中选了第一个 | 候选：D-029 通过后生效，A08 → `deviate`；取代 D-010 |
| D-024 | 2026-09-25 | **A09 + A12：维持 P1 的本地训练**：body `local_epochs: 5`，head `plocal_epochs: 1` + `head_lr_rep: 0.005`；**取代 D-012** | 总本地训练量已与论文相当：论文每端约 100 epoch（500 轮收敛 ≈ 50 epoch），P1 每端约 75–150 epoch（F-029）；head 的低 lr 是 `27d7d0dc` 针对 head 过拟合调出来的（F-030）。用户选择 | 生效（维持现状，无需改代码） |
| D-025 | 2026-09-25 | **A10：主协议保留标准化 + 增强**；「官方预处理」（无标准化、无增强）作对比实验组 G7（PLAN §4）。前提：`client_badpfl.py:53-56` 的 ε/σ 换算与 `attack/triggers.py` 跟随标准化开关（F-027） | 用户：「A10 可以作为一个小的对比试验，我认为作者代码的设定降低了一点攻击的难度」 | 生效（G7 与开关在 A4 实现、登记） |
| D-026 | 2026-09-25 | **A25：修取数缓存**：统一为官方语义 —— 每 epoch 重洗 + 重增强、`drop_last` 且尾批每 epoch 轮换；FedAvg、FedRep、生成器走同一个取数函数。配置开关，默认 = 现行为；并入 D-029 | F-025（seed42 下 3.2% 的训练样本从未参与训练）、F-026（G4 的数据流混淆）；用户选择「修，并入可行性实验」 | 生效（A4 实现，D-029 验证） |
| D-027 | 2026-09-25 | **A07 + A11：保留样本加权与现有划分**（`deviate`）；**S3 的新划分硬要求客户端等大小**，使恶意端数据占比 = n_malicious / n_clients，且加权与等权数值等价 | 标签偏斜同量级（F-031），为对齐而改无收益；大小不等使名义 10% 的恶意端在 P1 各布点的数据占比为 0.092–0.142（F-028），而布点是自变量。用户选择 | 生效（等大小由 S3 实现） |
| D-028 | 2026-09-25 | A13 保持 fp32（`deviate`）；A15 打开 `enable_op_determinism()`，并加 `[Checksum]` 行作验收（`align`）；A16 从 P2 配置删 `label_smoothing`（`align`）；**A22 目标标签固定 0**（`align`，= 官方代码、偏离论文）；A23 保留有效轮 floor 150 / cap 300（`deviate`）；A20 关闭（`done`） | A22：用户「毫无疑问的保留」。其余各行：用户批准 A2 计划时一并确认。理由逐行见 AUDIT | 生效（`align` 的在 A4 实现） |
| D-029 | 2026-09-25 | **可行性实验（A08 + A25）**：`flat_baseline` 与 `2edge_distributed` 各跑 seed42 × 1。判据先定后跑：`stop_reason=converged`，且 pm_acc 末 10 点 ≥ P1 同 seed 重复的最小值 − 0.006（flat ≥ 0.7367、2edge ≥ 0.7297）；ASR 只记录、不设门槛。flat 下 LR 不变 → 单独检验 A25；2edge 检验两者合并。登记在单独的 pilot 登记表（P1 口径、不进 P2），不违反 D-006。通过 → A08 改 `deviate`、A25 配 L1 后改 `done`；不通过 → 回审计重议 | 用户：改调过参的值要先用实验证明可行（D-022 ④）。P1 同 seed 重复（F-002 那几组）：flat pm_acc 0.7427 / 0.7428 / 0.7451；2edge_distributed 0.7357 / 0.7385 / 0.7414 / 0.7392。约 2.5 GPU-h | 待执行（A4） |
