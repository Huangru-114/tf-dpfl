# exp006 — badpfl × fedrep × resnet10，「对齐论文 fraction/poison」的尝试

- **配置（仓库内）**：`full_p4_resnet.yaml` = 100 client / 2 edge / 10 mal(spread) / cifar10 /
  hier_fedrep / defense=none / resnet10(BN) / **client_fraction=0.1 / poison_ratio=0.2** / 80 round。
- **恶意端**：编号为 11 整倍数的 10 个终端 = `[0,11,22,33,44,55,66,77,88,99]`。
- **metrics**：`exp006.metrics.json`
- **动机**：exp004 结论是「过度参与/过度投毒把主任务打崩」。本 run 把 fraction、poison
  降到论文值（0.1 / 0.2），看 acc 是否回到 ~0.72。**结果 pm_acc 仍崩到 0.4156。**

## 表面结果

| final | GM_ASR | EM_ASR | local_malicious | local_benign | same_edge | diff_edge | pm_acc |
|---|---|---|---|---|---|---|---|
| r80 | 0.969 | 0.959 | 0.916 | 0.440 | 0.440 | 0.000 | **0.4156** |

## ✅ 真正的根因（据 r47 / r79 原始训练日志，2026-08-23 用户提供）

> **先纠正一处此前的误判**：我曾据「10 恶意端 × 全部 80 轮参与」在 frac=0.1 下的
> 概率反推「exp006 没跑在 0.1 上」。**这是错的**，被原始日志证伪 —— 恶意端每轮全参与
> 不是因为 fraction≈1.0，而是因为**集群的 `select_clients` 强制把恶意端固定选进每一轮**。

原始日志揭示的选择行为（每个 edge_round、每个 global round 都一样）：

| edge | 每 edge_round 选中集合 | 恶意端 | 良性端 |
|---|---|---|---|
| **Edge 0**（"Selected **7**/50"）| 恒为 `{0,11,33,55,66,88,99}` | **7 个（全部）** | **0 个** |
| Edge 1（"Selected 5/50"）| 恒含 `{22,44,77}` + 2 个轮换良性 | 3 个（全部） | 2 个轮换 |

**选择语义（据日志推断）**：`n_select = max(int(frac·n_edge), n_malicious_in_edge)`，
恶意端**全部强制选入**，剩余名额才给随机良性。
- Edge 0：`max(int(0.1·50), 7) = max(5,7) = 7` → 7 恶意端**吃满**名额 → **良性端一个都进不来**。
- Edge 1：`max(5,3) = 5` → 3 恶意 + 2 随机良性。

### 由此推出 pm_acc 崩溃的机制（FedRep 特有）

**Edge 0 的 ~43 个良性端从头到尾从未被选中训练** → 在 FedRep 下，私有 head 只在客户端
被选中时本地训练 → **这些良性端的 head 永远停在初始化** → 个性化模型输出近乎常数类。
这正解释 log_tail 混淆矩阵**所有类塌向类 3**（未训练 head 的退化预测），以及
pm_acc 卡在 0.41（干净基线 0.72）。同时 Edge 0 的共享 backbone **100% 由攻击者更新**
（日志里 edge0 恶意端 loss ≈ 0.001–0.01，过拟合投毒），benign 无任何干净梯度进入。

### 为什么「降 client_fraction 到论文值」不但没用、反而伤害

降 frac → `int(frac·n_edge)` 名额更小 → 越容易被强制选入的恶意端吃满 → **越多良性端
被饿死**。这是**选择逻辑 × 拓扑**的接线问题，不是「投毒太狠」本身，也不是论文的设定
（论文里攻击者是被采样 cohort 的一部分，不是每轮强制 pin 进一个缩小的名额）。

## ✅ 机制来源（完全在仓库内，非代码漂移 —— 此前一度误判为「没 push」，已纠正）

`select_clients` 本身没有强制逻辑；强制来自 **`attack/backdoor.py:install_forced_participation`**
在**运行时给恶意端所在 edge 的 `select_clients` 打了包装器**（monkeypatch），`main.py:697-710`
在 backdoor 启用时对每个恶意端调用它。badpfl ∈ CONTINUOUS → `Q=1` → `round % 1 == 0` 恒真
→ **恶意端每轮强制在场**。包装器的规模回填循环（backdoor.py:208-210）`while len>target_n
and benign_in: remove benign` **只砍良性、绝不砍恶意** → edge0 的 7 恶意端 ≥ 名额 5，砍光良性
也降不到 5 → 选中集 = 7 恶意端、0 良性 → "Selected 7/50"。**全部可从仓库复现。**

## 次要澄清（非 bug）

- **diff_edge=0.0**：两个 edge 都含恶意端 → 没有 benign 属于「无恶意 edge」→ diff_edge
  样本集为空 → `_mean([])=0`（`attack/backdoor_eval.py:130-167`）。当前 config 下**跨 edge
  迁移性根本没被测到**，不能读成「跨 edge 迁移=0」。

## 本会话已落地（修复 + 收口可验证性，L1 全绿 174 passed / 6 skipped）

- **修复（用户确认：强制参与非有意默认，当前实验所有客户端应同概率被抽取）**：
  强制参与改为**默认关闭**，由新 config 键 `backdoor.forced_participation`（默认 false）门控。
  false → 不安装包装器 → 恶意端与良性端同概率被抽取；true → 保留原 Q 机制（投毒时间模式
  实验用的接口，用户明确要保留）。`main.py:697-716`。
- 自描述：`config_validate.py` 的 `[设定]` 行加 `forced_participation=…`；
  `harness/collect_metrics.py` 解析进 `run` 块。
- L1：`tests/test_forced_participation.py`（锁 wrapper 语义 + 恶意密集 edge 饿死良性的
  失败模式 + 关闭时同概率抽样，importorskip TF，集群跑）；`tests/test_collect_metrics.py`
  自描述 +2 条。configs 显式写 `forced_participation: false`（full_p4_resnet / config / smoke-base）。

## 下一步（turnkey，可复现）

集群 `git pull --rebase`，重跑仓库内 `full_p4_resnet.yaml`（现 forced_participation=false）：
```bash
sbatch run_full.sh attack bad-pfl badpfl none exp007 hier_fedavg_fedrep \
       experiments/attack/bad-pfl/full_p4_resnet.yaml
```
**验收前置**：新 metrics 的 `run.forced_participation==false`；日志里 edge0 的 "Selected N/50"
应含良性端（不再是恒定 7 全恶意），participation 显示恶意端约 40% 轮次而非 80/80。
过前置后再读 pm_acc（应回升，因良性 head 恢复训练）/ benign ASR —— 那才是问题1 的真正实验。
> 注意：同概率抽样下 badpfl 是连续攻击，每轮平均仅 ~1 恶意端在场，后门可能累积不起来——
> 那将是**诚实的科学结论**（在同等对待设定下迁移性弱），不要再用 forced_participation 去调它。
